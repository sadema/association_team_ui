import {useContext} from "react";
import {PlayerListContext} from "../player/PlayerListContext";

export const TeamSelect = () => {
    const {teamsByReference} = useContext(PlayerListContext);

    return (
        <>
        </>
    );
}
